# Core dependencies for verificacion-correo
playwright>=1.40.0
openpyxl>=3.1.2
pyyaml>=6.0.1

# Anti-detection dependencies (required for anti-detection features)
nodriver>=0.33.0
python-ghost-cursor>=0.1.0
fake-useragent>=1.5.0

# Build dependencies (for PyInstaller)
# Install separately: pip install pyinstaller>=6.0.0

# Note: tkinter, logging, pathlib, threading, queue, enum, dataclasses, typing
# are all part of Python standard library and don't need to be installed